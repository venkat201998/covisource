import React from 'react';

const Div = ({children}) => {
    return(
        <div className="form-group row justify-content-center">
            {children}        
        </div>
    )
}

export default Div;